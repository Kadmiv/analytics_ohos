name: analytics_ohos
description: "OpenHarmony / HarmonyOS NEXT Analytics Kit integration module"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.8.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  analytics_core:
    path: ../analytics_core
  logger: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
